vbdemo_src = []
vbdemo_incdir = include_directories('../include')

src_subdirs = ['drivers', 'gamemode', 'keypad', 'mainctl', 'sound', 'timer', 'video']

foreach s : src_subdirs
    subdir(s)
endforeach
